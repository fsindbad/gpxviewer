# GPX Viewer

A GPX track viewer: open a `.gpx` file to see the route on a map with distance, duration, elevation gain, and speed stats. Toggle sailing mode for a speed-colored track and VMG (velocity made good) relative to a set wind direction.

No build step — plain HTML/CSS/JS with [Leaflet](https://leafletjs.com/), deployed to Cloudflare Workers.
